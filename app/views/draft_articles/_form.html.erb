<%= form_with(model: draft_article) do |form| %>
  <% if draft_article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(draft_article.errors.count, "error") %> prohibited this draft_article from being saved:</h2>

      <ul>
        <% draft_article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
